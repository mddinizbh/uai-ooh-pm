# T9 — core.poi — POIs MULTI-FONTE (Overture + Foursquare + OSM) enriquecido

> Tarefa do Bloco 1 (uAI-OOH F1). **Auto-suficiente**: a sessão que rodar isto deve ler só os apontamentos abaixo + este arquivo.
> **Épico:** Épico 2 (2.2) · **Depende de:** T2 (schema core) + extensão do loader (abaixo) · **Paralelizável:** sim
> **Repo-alvo:** `uai-ooh-pipeline` · **cwd:** `~/IdeaProjects/personal/uai/uai-ooh-pipeline` · **Stack:** Python + PostGIS
> ⚠️ **Reescrita 2026-06-04** (workflow `ooh-poi-enriquecido`): de OSM-only para multi-fonte com nome/endereço/contato/porte. **NÃO deduplicar por proximidade** (decisão verificada — ver épico-2).

## Objetivo
Materializar `core.poi` multi-fonte para PERFIL do trajeto e PROSPECÇÃO de leads, a partir de `raw.overture__poi` (113.814) + `raw.fsq__poi` (132.061) + `raw.osm__poi` (2.478):
1. **Estender `load_poi_overture_fsq.py`** (no `uai-ooh-pipeline`) para trazer ao raw: endereço (locality/region/postcode), contato (tel/website/email/socials), `brand`+`confidence` (Overture). Hoje só traz nome/categoria/lat/lon.
2. Normalizar cada fonte → `core.poi` com **1 registro por POI por fonte** (identidade `(fonte, fonte_id)`; cada loja = 1 lead). **SEM colapso espacial.** Reprojeção 4326→31983.
3. Categoria via `core.poi_taxonomy` + `core.poi_category_map` (2 níveis: grupo_ooh + subgrupo). `relevancia_prospeccao` 0–3 (ruído como estradas/prédios = 0).
4. **Porte/faturamento: NÃO em F1** (adiado p/ v2 — decisão do dono 2026-06-04). `core.poi` sem coluna de porte.
5. (Opcional) `link_grupo_id` cross-source só com **sinal forte** (telefone/domínio/endereço iguais).
6. GiST em geom_31983 + btree de prospecção; `geom_geojson` (4326) p/ web.

## Apontamentos a LER antes de começar
- /Users/marleydiniz/IdeaProjects/personal/uai/uai-ooh-pm/docs/epicos/epico-2-contexto.md (Tarefa 2.2 — DDL completo, taxonomia, faturamento, decisão anti-dedup)
- /Users/marleydiniz/IdeaProjects/personal/uai/uai-ooh-pm/docs/proxies-e-premissas.md (regra de honestidade; adicionar a linha do porte estimado)
- /Users/marleydiniz/IdeaProjects/personal/uai/uai-ooh-pipeline/ooh_pipeline/ingestor/load_poi_overture_fsq.py (loader a estender)
- docs/epicos/runs/T2-schema-core-migrations.md

## Critério de pronto (verificável)
- Loader estendido carrega endereço+contato+brand no raw (rerun do `--load`).
- `core.poi` = **união marcada por fonte** (~248k; rotulado NÃO-deduplicado); contagens por `fonte` batem com o raw (FSQ 132.061 / Overture 113.814 / OSM 2.478).
- `tem_contato` populado (contato p/ prospecção, base legal = interesse legítimo + opt-out); `relevancia_prospeccao=0` captura o ruído (estradas/linhas de ônibus/prédios); distribuição por `grupo_ooh` plausível. **Sem coluna de porte em F1.**
- Honestidade carimbada: `fonte` explícita por POI; união NÃO-deduplicada rotulada. GiST criado.

## Índices na raw + persistência
- **Índices na raw (criar ao rodar):** temp-GiST apenas se houver cruzamento espacial (POI é mais transform que join). Padrão: geo = temp-table GiST; chaves de junção = `CREATE INDEX IF NOT EXISTS` + `ANALYZE` (deixar). Ver `arquitetura-servicos.md` §"Índices na `raw`".
- **Persistência:** `INSERT…SELECT` server-side; volume processado no Python → reusar a **classe de persistência em chunks** do pipeline.

## Apontamentos a PRODUZIR ao terminar
- docs/epicos/runs/T9-poi.md (contagens por fonte/grupo, cobertura de contato, % em 'outros', amostra de porte)
