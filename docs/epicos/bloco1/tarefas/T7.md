# T7 — core.vehicle + vehicle_line_history (auxiliar, adiável)

> Tarefa do Bloco 1 (uAI-OOH F1). **Auto-suficiente**: a sessão que rodar isto deve ler só os apontamentos abaixo + este arquivo.
> **Épico:** Épico 1 (1.5) · **Depende de:** T3 · **Paralelizável:** sim
> **Repo-alvo:** `uai-ooh-pipeline` · **cwd:** `~/IdeaProjects/personal/uai/uai-ooh-pipeline` · **Stack:** Python + PostGIS

## Objetivo
Materializar frota a partir de mco_consolidado + rt__vehicle_position (match veiculo=vehicle_id ~81%). Auxiliar: não alimenta score/impressões.

## Apontamentos a LER antes de começar
- /Users/marleydiniz/IdeaProjects/personal/uai/uai-ooh-pm/docs/epicos/epico-1-identidades.md (Tarefa 1.5)
- docs/epicos/runs/T3-line-trippattern.md

## Critério de pronto (verificável)
Frota distinta da 4107 = 31 carros (gate); ~81% com rt_vehicle_id. Pode ser feita por último ou adiada sem bloquear ninguém.

## Índices na raw + persistência
- **Índices na raw (criar ao rodar):** btree `raw.pbh__mco_consolidado(veiculo)` + ANALYZE. Padrão: geo = temp-table GiST; chaves de junção = `CREATE INDEX IF NOT EXISTS` + `ANALYZE` (deixar). Ver `arquitetura-servicos.md` §"Índices na `raw`".
- **Persistência:** `INSERT…SELECT` server-side; volume processado no Python → reusar a **classe de persistência em chunks** do pipeline.

## Apontamentos a PRODUZIR ao terminar
- docs/epicos/runs/T7-vehicle.md
