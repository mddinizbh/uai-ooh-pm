# T20a — `intel`: ITs (Testcontainers) + `mvn verify` (cobertura F1)

> Tarefa do Bloco 1 (uAI-OOH F1). **Auto-suficiente**: a sessão que rodar isto deve ler só os apontamentos abaixo + este arquivo.
> **Épico:** Épico 4 (4.6) · **Depende de:** T19 · **Paralelizável:** não
> **Repo-alvo:** `uai-ooh-intel` · **cwd:** `~/IdeaProjects/personal/uai/uai-ooh-intel` · **Stack:** Java 21 / Spring Boot

## Objetivo
Cobrir o `uai-ooh-intel` com ITs e fechar a cobertura F1. ITs com Testcontainers `postgres:16` (tabelas planas do schema `serving`, sem PostGIS): seed fixo de `serving.*`, testar `GET /api/lines/{id}/metrics` e `GET /api/lines/ranking` — **inclusive o re-rank por pesos e por público-alvo (AB/DE)** em memória sobre os 5 sub-scores. `mvn verify` verde.

## Apontamentos a LER antes de começar
- /Users/marleydiniz/IdeaProjects/personal/uai/uai-ooh-pm/docs/epicos/epico-4-serving-app.md (4.6 + Critério de pronto)
- /Users/marleydiniz/IdeaProjects/personal/uai/uai-ooh-pm/CLAUDE.md (§Testing; §Running tests on macOS — api.version=1.44, Java 21, Testcontainers 1.21.3)
- docs/epicos/runs/T19-serving-java.md

## Critério de pronto (verificável)
ITs de `/metrics` e `/ranking` (inclui re-rank por pesos e por público-alvo) passando; `mvn verify` (Java 21, JaCoCo ≥80%, api.version=1.44) verde; cobertura ≥80% linhas.

## Apontamentos a PRODUZIR ao terminar
- docs/epicos/runs/T20a-intel-its.md (ITs criados, cobertura final, resultado do `mvn verify`)
