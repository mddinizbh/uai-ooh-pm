# T19 — Épico 4 — serving Java no intel (endpoints F1, intel NOVO a partir do service-template)

> Tarefa do Bloco 1 (uAI-OOH F1). **Auto-suficiente**: a sessão que rodar isto deve ler só os apontamentos abaixo + este arquivo.
> **Épico:** Épico 4 (4.1-4.4) · **Depende de:** T18, T0b · **Paralelizável:** não
> **Repo-alvo:** `uai-ooh-intel` · **cwd:** `~/IdeaProjects/personal/uai/uai-ooh-intel` · **Stack:** Java 21 / Spring Boot
> ⚠️ **Decisão do dono 2026-06-04:** o `intel` é um repo NOVO, do zero, a partir do `uai-ooh-service-template` (T0b). O `uai-bus-lines-map` fica CONGELADO — NÃO é fork nem rename. Como o bus-lines não é mais o backend, o intel **reimplementa também o catálogo** (linhas/shapes/pontos), não só as métricas.

## Objetivo
Implementar o `uai-ooh-intel` como serviço NOVO (do zero, a partir do `uai-ooh-service-template`): leitura do schema `serving` **no próprio ooh-postgis** (usuário `ooh_intel_ro`, SELECT plano, sem ST_*), domínio (records `LineMetrics`/`LineImpressions`/`DemographicProfile`, sealed `ClasseRenda`, `ScoreBreakdown`), portas `lineMetrics(id)`/`rankLines(weights)`, persistência e endpoints. Servir **catálogo F1** (linhas, `line_shape` GeoJSON, `line_stop`) **+** ficha (`GET /api/lines/{id}/metrics`) **+** ranking (`/api/lines/ranking?weights=&publicoAlvo=`). NÃO tocar o `uai-bus-lines-map`.

## Apontamentos a LER antes de começar
- /Users/marleydiniz/IdeaProjects/personal/uai/uai-ooh-pm/docs/epicos/epico-4-serving-app.md (4.1-4.4)
- /Users/marleydiniz/IdeaProjects/personal/uai/uai-ooh-pm/docs/plano-normalizacao-core.md (§Consumo pelo app Java)
- src/main/java/com/uai/buslines/domain/port/in/QueryNetworkUseCase.java
- docs/epicos/runs/T18-infra-vps.md

## Critério de pronto (verificável)
endpoints respondem do schema `serving` no ooh-postgis sem tocar PostGIS (sem ST_* em runtime); catálogo (linhas/shapes/pontos) + ficha + ranking servidos pelo intel novo; re-rank por pesos e por público-alvo em memória sobre os 5 sub-scores; build compila; ITs verdes; OpenAPI atualizado; `uai-bus-lines-map` intocado e ainda live. (Decisão do dono 2026-06-04: serving no ooh-postgis, sem export pro uai_buslines — reescrever o épico-4 nessa linha.)

## Apontamentos a PRODUZIR ao terminar
- docs/epicos/runs/T19-serving-java.md
