# T14 — Demanda por linha (MCO volume + embarque distribuição)

> Tarefa do Bloco 1 (uAI-OOH F1). **Auto-suficiente**: a sessão que rodar isto deve ler só os apontamentos abaixo + este arquivo.
> **Épico:** Épico 3 (3.4) · **Depende de:** T3 · **Paralelizável:** sim
> **Repo-alvo:** `uai-ooh-pipeline` · **cwd:** `~/IdeaProjects/personal/uai/uai-ooh-pipeline` · **Stack:** Python + PostGIS

## Objetivo
line_demand_daily (volume MCO set/2025 por tipo_dia) + line_boarding_profile (curva horária normalizada do embarque mai/2024, distribuição não volume).

## Apontamentos a LER antes de começar
- /Users/marleydiniz/IdeaProjects/personal/uai/uai-ooh-pm/docs/epicos/epico-3-metricas-score.md (Tarefa 3.4)
- docs/epicos/runs/T3-line-trippattern.md

## Critério de pronto (verificável)
4107: útil 3.616 pax/dia, sáb 1.544, dom 920 (gate-faixa); linha→short_name ~99%; embarque normalizado por linha.

## Índices na raw + persistência
- **Índices na raw (criar ao rodar):** btree `raw.pbh__mco_consolidado(linha, tipo_dia)` + ANALYZE. Padrão: geo = temp-table GiST; chaves de junção = `CREATE INDEX IF NOT EXISTS` + `ANALYZE` (deixar). Ver `arquitetura-servicos.md` §"Índices na `raw`".
- **Persistência:** `INSERT…SELECT` server-side; volume processado no Python → reusar a **classe de persistência em chunks** do pipeline.

## Apontamentos a PRODUZIR ao terminar
- docs/epicos/runs/T14-demanda.md
