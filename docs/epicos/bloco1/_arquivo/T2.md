# T2 — Schema core como migrations versionadas + core.model_params (absorve Tarefa 1.0)

> Tarefa do Bloco 1 (uAI-OOH F1). **Auto-suficiente**: a sessão que rodar isto deve ler só os apontamentos abaixo + este arquivo.
> **Épico:** Épico 1 (Tarefa 1.0) · **Depende de:** T1 · **Paralelizável:** não
> **Repo-alvo:** `uai-ooh-pipeline` · **cwd:** `~/IdeaProjects/personal/uai/uai-ooh-pipeline` · **Stack:** Python + PostGIS

## Objetivo
Materializar o schema core no banco ooh local como migrations versionadas idempotentes (ferramenta a definir — sqitch nativo Python, evita JVM no container) em vez de CREATE SCHEMA IF NOT EXISTS solto. Criar core.dataset_version (alinhar vocabulário de status com o app: ACTIVE/BUILDING/ARCHIVED) e core.model_params (DDL via migration + seed dos coeficientes/pesos via UPSERT idempotente recarregável).

## Apontamentos a LER antes de começar
- /Users/marleydiniz/IdeaProjects/personal/uai/uai-ooh-pm/docs/epicos/epico-1-identidades.md (Tarefa 1.0)
- /Users/marleydiniz/IdeaProjects/personal/uai/uai-ooh-pm/docs/plano-normalizacao-core.md (§Schema core; §Modelo de impressões/score)
- `git show legacy-frozen:src/main/resources/db/migration/V1__create_tables.sql` (referência do dataset_version do app legado — removido do HEAD, na tag legacy-frozen)
- docs/epicos/runs/T1-baseline-modelo.md

## Critério de pronto (verificável)
Migration baseline aplica core + dataset_version + model_params no ooh local sem DROP+CREATE; runner de migration escolhido e documentado; uma dataset_version BUILDING ativa; model_params populado com f_exposicao=0.15, p_vista_traseira=0.6 e os pesos do score (0.35/0.15/0.20/0.20/0.10), com COMMENT 'não calibrado'.

## Apontamentos a PRODUZIR ao terminar
- docs/epicos/runs/T2-schema-core-migrations.md (runner escolhido, estado do schema, seed model_params)
