/**
 * Driven ports — repository and gateway interfaces implemented by adapters.
 *
 * <p>Populated in task_04 (persistence adapter). Primary interface:
 * {@code NetworkDatasetRepository} (transactional dataset swap).
 */
package com.uai.buslines.domain.port.out;
