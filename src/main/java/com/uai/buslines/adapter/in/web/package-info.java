/**
 * Web adapter — REST controllers and response DTOs.
 *
 * <p>Implements the driving ports defined in {@code domain.port.in}.
 * Populated in task_06 (Read API + OpenAPI). Error handling via
 * {@code @ControllerAdvice} returning structured JSON (uAI convention).
 */
package com.uai.buslines.adapter.in.web;
