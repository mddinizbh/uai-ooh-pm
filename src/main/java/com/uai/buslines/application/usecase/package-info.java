/**
 * Application layer — use case implementations (orchestrate domain + ports).
 *
 * <p>Populated in tasks 05 (import orchestration) and 06 (query use cases).
 * No framework dependencies allowed in this package.
 */
package com.uai.buslines.application.usecase;
