# ── API Dockerfile context (root) ─────────────────────────────────────────────
# Excludes everything that is not needed to build the API JAR.

# Frontend (has its own Dockerfile under web/)
web/

# Maven build output (rebuilt inside the container)
target/

# IDE / tool files
.idea/
.git/
.github/
.claude/
.compozy/

# Deploy snippets
deploy/

# Documentation
*.md
LICENSE

# OS artefacts
.DS_Store
