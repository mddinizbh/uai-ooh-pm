# ── SPA Dockerfile context (web/) ─────────────────────────────────────────────
# Excludes heavyweight local artefacts from the Docker build context.

# Reinstalled inside the container with `npm ci`
node_modules/

# Previous build output (rebuilt inside the container)
dist/

# Test coverage reports
coverage/

# Documentation
*.md

# OS artefacts
.DS_Store
